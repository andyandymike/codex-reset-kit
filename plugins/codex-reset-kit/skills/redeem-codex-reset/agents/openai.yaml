interface:
  display_name: "Codex Reset Remote"
  short_description: "Use a reset credit safely from your phone"
  default_prompt: "Use $redeem-codex-reset to check whether this computer is ready for phone Remote resets, without using a credit."

policy:
  allow_implicit_invocation: false
